package sqlbuilder

import (
	"testing"
)

func TestStatistics(t *testing.T) {
	// testutils.InitTestConfig()
	// r := &StatisticsTimeRange{
	//   Start: time.Now().UnixNano(),
	//   PointCount: 10,
	//   Align: true,
	//   Type: RangeType_Minute,
	//   Interval: 1,
	// }
	// ranges := r.ParseRanges()
	// for _,r := range ranges{
	//   log.Debug("range",zap.Time("start",time.Unix(0,r.GetStart())),zap.Time("end",time.Unix(0,r.GetEnd())))
	// }
}
